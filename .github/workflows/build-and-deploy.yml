name: Daily AI Package Blog Generation & Jekyll Deploy (DEPRECATED)

# ============================================================================
# DEPRECATED: This workflow is replaced by daily-best-of-the-best.yml
# which handles blog generation, image creation, Jekyll build, and deployment.
# Keeping this file disabled to preserve history.
# ============================================================================

on:
  workflow_dispatch:  # Manual trigger only (no longer scheduled)

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    if: false  # Disabled - use daily-best-of-the-best.yml instead

    steps:
      - name: This workflow is deprecated
        run: |
          echo "This workflow has been replaced by daily-best-of-the-best.yml"
          echo "Please use that workflow instead."
          exit 0
